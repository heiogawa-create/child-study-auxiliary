services:
  - type: web
    name: study-hint-app
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: node server.mjs
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false
      - key: NODE_ENV
        value: production
